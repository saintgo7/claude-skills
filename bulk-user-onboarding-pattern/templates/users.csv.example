# bulk-users 입력 형식 — username,email,plan,[device_label]
# `#` 시작은 주석, 빈 줄은 skip.
# device_label 이 있으면 사용자 등록 후 API 키도 함께 발급.

alice,alice@example.com,pro,laptop
bob,bob@example.com,free
carol,carol@example.com,pro,ci-cd
dave,dave@example.com,free,
