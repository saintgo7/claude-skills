# gem-llm-admin-cli

GEM-LLM 사용자/API 키 관리 CLI 스킬. raw_key는 발급 직후 한 번만 표시되므로 즉시 안전 보관 필수.

## 사용 시점

- "사용자 추가", "키 발급/회수"
- "사용량 조회", "alice 한테 키 줘"
- "bulk 사용자 등록", "사용자 목록"
- "권한 관리"

## 설치

```bash
./install.sh gem-llm-admin-cli
```

명령어 목록과 직접 admin REST API 호출 예시는 [SKILL.md](SKILL.md) 참조.
